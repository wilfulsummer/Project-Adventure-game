# Git ignore file itself
.gitignore

# Note: .cursorrules is intentionally committed for development guidelines

# Save files and directories
savegame.json
saves/
*.save

# Unique items tracking (should not be tracked by git)
unique_items.json

# Mods directory (intentionally committed for other developers to learn from)
# mods/ - REMOVED: This directory is now committed for educational purposes

# Python cache files (should always be ignored)
__pycache__/
*.py[cod]
*$py.class

# Bug reports (files ignored, folder committed for transparency)
bug_reports/*.txt
bug_reports/*.log

 